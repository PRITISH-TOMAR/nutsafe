# placeholder — fill in during collector setup
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4137
      http;
        endpoint: 0.0.0.0:4138

exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [debug]